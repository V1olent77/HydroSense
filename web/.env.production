# Set this on Vercel via Project → Settings → Environment Variables
# (overrides whatever's in the file). Local placeholder so the build
# doesn't fail if no env var is set.
VITE_API_BASE=https://hydrosense-api.onrender.com
